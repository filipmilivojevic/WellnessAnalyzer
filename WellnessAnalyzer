import java.io.File;
import java.io.FileNotFoundException;
import java.util.*;

public class WellnessAnalyzer {
   public static void main(String[] args) throws FileNotFoundException {

       ArrayList<Person> people = new ArrayList<>();
       //TODO - fill in the following, to read data from a file, use the data to construct
       // specific Person objects (Student, Teacher, and Staff), and store in a List of Person (polymorphism)
       Scanner file = ;  //construct Scanner object using appropriate filename (such as wellness_data.csv)
       file.nextLine(); // skip header line.

       while ()) { //add appropriate while condition here.
           String line = // code to read the next line from the .csv file
           String[] parts = // code to split the line into an array of String, using commas as delimiters (separators)

           String role = // store the first item in a variable
           String name = // store the second item in a variable
           int steps =  // store the third item in a variable
           double sleep = //store the fourth item in a variable - need to do math on this one, so parse as a double
           double water = //store the fifth  item in a variable - need to do math on this one, so parse as a double

           if (role.equals("student")) {
               //construct Student object, and add to list of Persons.  This is polymorphism.
           } else if (role.equals("teacher")) {
               //construct Teacher object, and add to list of Persons.  This is polymorphism.
           } else if (role.equals("staff")) {
               //construct Saff object, and add to list of Persons.  This is polymorphism.
           }
       }

       // TODO:
       // - compute statistics
       // - print results
   }
}
